<%- include('../partials/head') -%>
  <main class="container px-3">
    <div>Welcome back
      <% data.forEach(function(user) { %>
        <b><%= user.name %></b>
      <% }); %>
    </div>
  </main>
<%- include('../partials/footer') -%>